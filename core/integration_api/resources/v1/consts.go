package v1

const (
	errorKey               = "error"
	errBindBody            = "bind body error: %s"
	errAuthIsMissing       = "Authorization header is missing"
	errAuthTokenIsNotValid = "Authorization header token is not valid"
	errGetQuery            = "get query error"
)

const (
	StoreTokenKey = "store_token"
)

const (
	Authorization = "Authorization"
	Token         = "token"
	Bearer        = "Bearer"
)
